/**
 * `deveye-sdk-client` — the runtime barrel the app provides when your module
 * is compiled in. Its typed portrait ships WITH `deveye-types` (one source of
 * truth, mechanically checked against the real barrel by DevEye's own CI);
 * this stub only pulls that ambient declaration into this repo's standalone
 * typecheck. Nothing to maintain here.
 */
/// <reference path="../node_modules/deveye-types/src/sdk/client-ambient.d.ts" />
